services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    command: mongod --replSet rs0 --bind_ip_all
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "try { rs.status().ok } catch(e) { rs.initiate({ _id: 'rs0', members: [{ _id: 0, host: 'mongo:27017' }] }).ok }"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data

  server:
    image: node:20-alpine
    working_dir: /app
    restart: unless-stopped
    depends_on:
      mongo:
        condition: service_healthy
    ports:
      - "5000:5000"
    environment:
      NODE_ENV: development
      PORT: 5000
      MONGO_URI: mongodb://mongo:27017/splitchill?replicaSet=rs0
      MONGODB_URI: mongodb://mongo:27017/splitchill?replicaSet=rs0
      CLIENT_URL: http://localhost:5173
      JWT_SECRET: local-dev-secret-change-me
      JWT_REFRESH_SECRET: local-dev-refresh-secret-change-me
      REDIS_URL: redis://redis:6379
      SOCKET_REDIS_URL: redis://redis:6379
      HYPERSWITCH_BASE_URL: http://host.docker.internal:8080
      HYPERSWITCH_API_KEY: dev-hyperswitch-api-key
      HYPERSWITCH_WEBHOOK_SECRET: dev-hyperswitch-webhook-secret
      TEXTBEE_BASE_URL: https://api.textbee.dev/api/v1
      TEXTBEE_API_KEY: ""
      TEXTBEE_DEVICE_ID: ""
      TEXTBEE_WEBHOOK_SECRET: dev-textbee-webhook-secret
    volumes:
      - ./server:/app
    command: sh -c "npm install && npm run dev"

  client:
    image: node:20-alpine
    working_dir: /app
    restart: unless-stopped
    depends_on:
      - server
    ports:
      - "5173:5173"
    environment:
      VITE_API_URL: http://localhost:5000/api
      VITE_SOCKET_URL: http://localhost:5000
    volumes:
      - ./client:/app
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"

volumes:
  mongo-data:
  redis-data:
